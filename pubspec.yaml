name: iptv_player
description: Professional IPTV M3U Player
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  url_launcher: ^6.2.1
  video_player: ^2.8.0
  wakelock_plus: ^1.1.4
  shared_preferences: ^2.2.2
  webview_flutter: ^4.8.0   # <--- ADD THIS

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true